# Lesson 2 — Reading a data-quality report

A coverage rate is the share of the target population a collection actually reached.
This markdown file is a second real lesson on disk, so a test can confirm the reader
returns two lessons and streams each with its own content type.
